import { createTheme  } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    secondary: {
      light: '#757ce8',
      main: '#fff',
      dark: '#002884',
      contrastText: '#fff',
    },
    primary : {
      light: '#FFD700',
      main: '#FFD700',
      dark: '#FFD700',
      contrastText: '#fff',
    },
  },
});

